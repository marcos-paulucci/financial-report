export class Track {
    id: string;
    name: string;
    label: string;

    constructor (id?: string, name?: string, label?: string){
        this.id = id;
        this.name = name;
        this.label = label;
    }
};

